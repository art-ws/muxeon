{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "Repo type-check project: checks every package's src + tests in one pass. Cross-package `@muxeon/*` imports resolve to source via `paths` (and bun workspace symlinks).",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@muxeon/*": ["packages/*/src/index.ts"]
    }
  },
  "_commentExclude": "webchat-ui is a browser SPA (§12.7): DOM libs + JSX — checked by its own tsconfig (packages/webchat-ui/tsconfig.json, `bun run typecheck` there / in its build).",
  "include": ["packages/*/src/**/*.ts", "packages/*/test/**/*.ts", "tools/**/*.ts"],
  "exclude": ["packages/webchat-ui"]
}
