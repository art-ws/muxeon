# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Runtime state — queues, routine state and panel history live under <config_dir>
/queue/
/state/
/webchat/

# Local config (ship only muxeon.config.example.json)
/muxeon.config.json

# Secrets (channel secrets are $env-only)
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.idea/
.vscode/
*.log
