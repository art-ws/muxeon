// @teamai/signals — Signal/kind envelope construction + on-demand send via the
// router (§3.3, §5.3, FR-18/FR-19). Consumed by routines (§6) and the operator-plane
// signals.send (§8.5). — T24.
export * from "./envelope";
export * from "./send";
