{
  "$schema": "https://json.schemastore.org/tsconfig",
  "_comment": "UI-local type-check (§12.7): browser libs + JSX on top of the shared base. The root tsconfig checks only .ts — components are checked here (`bun run typecheck` in this package / the build).",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["bun"]
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "test/**/*.ts", "build.ts"]
}
