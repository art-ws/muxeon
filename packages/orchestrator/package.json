{
  "name": "@teamai/orchestrator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Execution core: dispatcher (dequeue→inject→await idle→complete), AgentStatus state-machine, router (topology-checked enqueue for all producers). Owns §10.1/§10.2/§10.8. Sole consumer of @teamai/queue.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@teamai/core": "workspace:*",
    "@teamai/config": "workspace:*",
    "@teamai/queue": "workspace:*",
    "@teamai/adapters": "workspace:*",
    "@teamai/tmux": "workspace:*"
  }
}
